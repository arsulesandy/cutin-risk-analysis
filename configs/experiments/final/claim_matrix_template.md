# Claim Matrix Template

Status values:

- `todo`: claim exists but evidence is not finalized
- `in_progress`: experiments or analysis running
- `done`: evidence complete and linked

Mark a claim as `done` only when:

1. Metric and split protocol are explicitly stated.
2. Artifact path exists and is reproducible.
3. A short interpretation paragraph is added to thesis text.
